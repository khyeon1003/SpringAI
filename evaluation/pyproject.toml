[project]
name = "springai-ragas-evaluation"
version = "0.1.0"
description = "Simple RAGAS evaluation for the Spring AI chatbot"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
    "ragas>=0.4.0",
]

[tool.uv]
package = false
